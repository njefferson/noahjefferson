## 206 · A cold reader with only the release claims finds what green gates and the device both miss

**Enforced by:** CHECKLIST cold-read-back — for a release worth the cost, write
each claim as a place to stand and a thing that must be visible there, then
have a second session with no build context walk the deployed app holding only
that text, forbidden the source, and diff its report against intent. ·
JUDGEMENT — its disagreement is strong evidence and its agreement is weak: a
sibling model reads charitably and shares the author's blind spots, so a match
certifies nothing about a human and a miss is a defect caught cheaply.

**Smell:** a release where every gate is green, the device pass found nothing,
and nobody has checked whether a stranger holding the release notes could FIND
each claimed thing. Also: routing a cold reader's findings to the owner as
"product decisions" when the record can answer them — the owner's name for
that was fake carefulness, and it is §96's deference shape pointed the other
way.

**Quietkeep 3.20.x, 2026-09-01.** First run of the method, designed by the
owner as the read-back half of a handover (the app's own research catalogue,
entry 33: the measured remedy for divergence is not a better document but the
receiver saying back what they understood). One agent, one browser, the
deployed bundle, the seven claims, about the tokens of one long report on the
mid-tier model. It found, inside the hour: an import that opened a waiting in
the data while every owed surface stayed blind to it, because the surfaces key
on a KIND the row did not get; a focus-ring rule announced as "every sheet"
that covered one title styling and missed the most-opened sheet in the app; a
dependency picker offering people and places as things work could queue
behind, because one of two sibling pickers had grown its own filter and
forgotten kind; a walkthrough pointer to a door that no longer held what it
promised; and a notes surface that hid the larger of two releases landing in
one promote. Every gate was green throughout, the item sheet had been walked
by the accessibility gate in both themes that same hour, and the owner's own
device pass had just been through the same screens. Three of the five were
fixed the same day, two the next hour after the owner overruled their routing.
The isolation is by instruction, not by wall — the agent declares what it
consulted, and a leak invalidates the run, not the method.
